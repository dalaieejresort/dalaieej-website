I need to add a "Visual Journey" Gallery feature to the site, inspired by luxury hotel sites like Badrutt's Palace.

Please implement the following 3 steps:

STEP 1: CONFIGURATION
Add this `galleryConfig` to my `siteConfig` (or a new config file) so we can manage the categories centrally:

export const galleryConfig = {
  title: { en: "Visual Journey", mn: "Дурсамж Гэрэл Зурагт" },
  description: { en: "Immerse yourself in the rhythm of Khuvsgul.", mn: "Хөвсгөл нуурын байгалийн сайхныг мэдрээрэй." },
  categories: [
    { id: "all", label: { en: "ALL", mn: "БҮГД" } },
    { id: "resort", label: { en: "THE RESORT", mn: "ОРЧИН" } },
    { id: "rooms", label: { en: "ACCOMMODATIONS", mn: "ӨРӨӨНҮҮД" } },
    { id: "dining", label: { en: "CULINARY", mn: "ЗООГ" } },
    { id: "wellness", label: { en: "WELLNESS", mn: "АМРАХУЙ" } },
    { id: "adventures", label: { en: "ADVENTURES", mn: "АЯЛАЛ" } },
    { id: "landscape", label: { en: "THE LAKE", mn: "БАЙГАЛЬ" } }
  ]
};

STEP 2: THE GALLERY PAGE (`app/[locale]/gallery/page.tsx`)
Create a new page at `/gallery`.
- **Header:** Use the `title` and `description` from the config as the H1/Subtitle.
- **Filter Bar:** Create a row of "Pill" shaped buttons for the categories.
- **Grid Layout:** Use a "Masonry" layout (like Pinterest) where images of different sizes fit together cleanly. 
- **Interaction:** - Default view shows "ALL".
  - Clicking a category filters the images instantly (client-side filtering).
  - Clicking an image should open it in a simple Lightbox (full-screen modal).
- **Placeholder Data:** Since I don't have all photos yet, please create an array of 12 placeholder images with different aspect ratios (some tall, some wide) and assign them random categories from the config so I can test the filtering.

STEP 3: THE HEADER ICON
Update my main `Navbar` component.
- Add a new "Grid Icon" (looks like 4 small squares or 3x3 dots) to the right side of the navbar, near the Language Switcher or "Book Now" button.
- The icon should link to `/gallery`.
- On hover, it should show a tooltip or small text saying "Gallery".